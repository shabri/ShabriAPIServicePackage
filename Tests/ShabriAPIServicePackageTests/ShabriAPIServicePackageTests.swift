import XCTest
@testable import ShabriAPIServicePackage

final class ShabriAPIServicePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ShabriAPIServicePackage().text, "Hello, World!")
    }
}
